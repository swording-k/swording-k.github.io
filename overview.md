# 中式剑气电影感背景升级（重做版）— 概述

## 问题与目标

第一版被用户反馈为「只是把一张方块图片半透明放在后面，没有电影感」。核心缺陷：

1. 使用的天罡剑 PNG 是 RGB 无透明通道，背景是一块深色矩形画布，滚动时能看到明显边界。
2. 剑身缩放太小（scale ≈ 1.45），没有「巨剑填满视口」的冲击感。
3. 镜头 yPercent 只移动了约 35%，没有真正沿剑身从剑柄走到剑尖。

本版目标是：让宝剑成为贯穿全站的巨型背景，滚动时像电影摄影机一样沿剑身上下移动，每屏对应剑的不同部位，并彻底消除「方块贴图」感。

## 做了什么

- **新增透明底剑图**：基于 `images/kunwu-heaven-pillar-concept-v2-wide-guard.png` 用亮度+色度键控生成 `images/kunwu-heaven-pillar-transparent.png`，保留原始文件不变。透明底让剑身真正融入页面暗色背景，不再是一块黑底图片。
- **重写 `css/sword-background.css`**：
  - 剑身容器放大到 `300vh`（桌面大屏 `340vh`），并配合 `scale(2.4~2.7)`，让剑身远大于视口，形成「身处巨剑之中」的电影构图。
  - 新增 `.sword-ghost` 模糊剑体虚影，用 `mix-blend-mode: screen` 填充画面氛围。
  - 强化暗角、金色/青色径向光晕、缓慢飘动的剑气薄雾。
  - 刃面扫光、移动端/小屏/`prefers-reduced-motion` 降级。
- **修改 `index.html`**：
  - 引用透明剑图并加缓存戳 `?v=2`。
  - 加入 `.sword-ghost` 与 `.sword-mist` 结构。
  - CSS/JS 版本号升级为 `?v=20260724b` / `?v=20260724b-sword` 破缓存。
- **重写 `js/main.js` 的 `initGlobalSwordCinematic()`**：
  - 新关键帧 scale 提升到 2.35–2.70，yPercent 从 `+40`（剑柄/护手）走到 `-40`（剑尖），真正覆盖整剑。
  - 使用 `gsap.matchMedia()` 做桌面/移动端分级，桌面端保留鼠标微视差。
  - 减少动效环境下 JS 直接退出，CSS 接管为静态背景。

## 关键决策

1. **不做 3D 模型**：继续 2D 透明渲染图 + GSAP scroll-driven 摄影机，性能与审美可控。
2. **保留中式天罡剑**：通过键控解决背景不透明问题，而不是换成西式 Longclaw，符合作者偏好。
3. **全站固定背景层**：不影响现有 hero、作品、能力、联系等任何内容结构；内容层 `z-index: 1` 始终浮于剑气之上。
4. **可读性优先**：剑身整体调暗（brightness ~0.84），并用强暗角把视线锁在中央；项目卡片自身带半透明背景，文字不受影响。

## 镜头关键帧（桌面）

| 滚动进度 | xPercent | yPercent | scale | rotation | 画面位置 |
|---|---|---|---|---|---|
| 0    | -16 | +40 | 2.40 | -1.6° | 剑柄/护手 |
| 0.22 |  +8 | +20 | 2.55 | +0.9° | 护手过渡 |
| 0.45 |  -6 |  +4 | 2.70 | -0.7° | 中段剑身 |
| 0.70 | +10 | -18 | 2.55 | +0.8° | 下段剑身 |
| 1.0  |   0 | -40 | 2.35 |  0.0° | 剑尖 |

移动端通过 `motionScale = 0.78` 等比压缩 scale/x/y/rotation 幅度。

## 测试与降级

- 桌面端：剑身容器 300vh，scale 2.4–2.7，刃面 7.2s 呼吸扫光，鼠标微视差。
- 大屏桌面（≥1600px）：容器 340vh，scale 2.65。
- 平板（≤1024px）：容器 280vh，scale 2.1。
- 移动端（≤760px）：全局透明度 0.42，scale 1.85，运动幅度 0.78。
- 小屏（≤480px）：全局透明度 0.32，scale 1.6。
- `prefers-reduced-motion: reduce`：关闭动画，静态 scale 1.9，透明度 0.26。
- JS 初始化包在 `try/catch` 内，GSAP 不可用时自动降级。

## 后续可优化项

- 可继续微调剑身亮度/暗角，或给某些内容卡片增加更实的底，以在剑身最亮处保证文字可读性。
- 可作为对比切出 `feat/sword-cinematic-western` 分支，用 Longclaw 或 Valyrian 再试一条西式路线。
- 可进一步加入愉悦体验设计师的微观打磨：卡片悬停光泽、按钮磁性反馈文案、加载/空状态微文案等。

## 提交

- 分支：`feat/sword-cinematic-bg`
- 本次重做提交：`<待写入>`
- 变更文件：`index.html`、`js/main.js`、`css/sword-background.css`、`images/kunwu-heaven-pillar-transparent.png`、`overview.md`
